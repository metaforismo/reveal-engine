# Evidence ledger

Repository-level engineering evidence from 2026-07-29, branch
`platform/core`, at package version 0.4.0, with `progressive-market`,
`sequential-cards`, `staged-survival` and `permutation` all registered. Every
figure below was produced by the command in its row on this working tree, after
the three module branches were integrated. It does not cross the certification
boundary.

## Suite-wide

| Claim / control                                                                               | Reproducible evidence                                                                                                                                                                                            | Observed result                                                                                                                                                                                                                                                                                                                                                                                         |
| --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Stable API, strict codecs, fixtures, exact math, protocol, replay, races, and hostile inputs  | `npm test`                                                                                                                                                                                                       | 43 files / 893 tests passed                                                                                                                                                                                                                                                                                                                                                                             |
| Coverage gate                                                                                 | `npm run test:coverage`                                                                                                                                                                                          | statements 90.17%, branches 84.21%, functions 97.84%, lines 90.93%                                                                                                                                                                                                                                                                                                                                      |
| Build and package surface                                                                     | `npm run typecheck && npm run build && npm run test:package`                                                                                                                                                     | tarball installed into a throwaway consumer; 14 subpaths resolved through the published `exports` map, one per module plus `./modules/permutation/aether`; 205 packed files; no src/tests                                                                                                                                                                                                               |
| Proof vectors, migrations, tamper taxonomy, snapshot mutation, and hostile request validation | `npm run test:security`                                                                                                                                                                                          | 185 focused tests passed (105 hostile-input, 38 snapshot-mutation, 34 staged-survival hostile-input, 8 proof-vector); `npm audit --omit=dev` found 0 vulnerabilities                                                                                                                                                                                                                                    |
| Lifecycle-module contract is implementable by more than its first client                      | `tests/lifecycle-module-contract.test.ts`                                                                                                                                                                        | 51 tests; a test-only permutation-truth / zero-elimination / combinatorial-paytable / multi-claim module passes its own conformance run                                                                                                                                                                                                                                                                 |
| The contract carries a round whose transcript depends on player decisions                     | `tests/choice-timed-contract.test.ts`                                                                                                                                                                            | 14 tests; a `choiceTiming: 'before-step'` module with a seed pre-commitment, a bound choice log, and per-entity partial banking                                                                                                                                                                                                                                                                         |
| Round payout never exceeds `maxWinMultiple` x money brought in                                | `tests/cap-chain-invariant.test.ts`                                                                                                                                                                              | invariant checked after every command over all 7,380 sequences up to length four **through both credit paths** (`creditClaim` and the `creditWithinCap` + `applyCredit` pair, required to agree exactly), plus 5,000 seeded interleavings up to length 20                                                                                                                                               |
| Every shipped wire format is frozen, not round-tripped                                        | `tests/frozen-fixtures.test.ts`, `tests/sequential-cards/frozen-fixtures.test.ts`, `tests/staged-survival-fixtures.test.ts`, `tests/permutation-frozen-fixtures.test.ts`, `tests/aether-frozen-fixtures.test.ts` | 7 + 8 + 6 + 10 + 3 tests rebuild the round in `beforeAll` and compare committed bytes field for field                                                                                                                                                                                                                                                                                                   |
| Independent probability, pricing, strategy, rounding, cap, and accounting oracles             | `tests/math-oracle-invariants.test.ts`, `tests/protocol-accounting.test.ts`                                                                                                                                      | exhaustive binary paths plus three-adapter raw-weight checks passed                                                                                                                                                                                                                                                                                                                                     |
| Deterministic property/metamorphic coverage                                                   | `tests/property-metamorphic.test.ts`, `tests/sequential-cards/property-metamorphic.test.ts`                                                                                                                      | 34 + 16 tests: 32 fixed property seeds plus 256 replay/isolation cases, and the same discipline over the card lifecycle                                                                                                                                                                                                                                                                                 |
| A re-sealed snapshot cannot rewrite what a player bet or what it is worth                     | `tests/security/snapshot-mutation.test.ts`, `npm run conformance`                                                                                                                                                | 38 tests; every re-sealed field mutation rejected on its merits, plus two end-to-end cases pinning the settlement each attack was worth. Conformance repeats the tamper sweep across all 13 definitions: 336 progressive-market snapshots, 1,120 sequential-cards snapshots and 56 dormant settlements, 156 staged-survival snapshots and 22 transcripts, 768 permutation snapshots and 192 transcripts |
| Core primitives: weights, counting, permutations, tapes, sealing, ledger                      | `tests/core-primitives.test.ts`                                                                                                                                                                                  | 39 focused tests passed, including zero-elimination, a fail-closed claim budget, cap-basis accumulation, exact 1/336 trifecta pricing, and duplicate-key rejection on restore                                                                                                                                                                                                                           |
| Module generality and conformance                                                             | `npm run conformance`                                                                                                                                                                                            | registry-driven: 4 modules x 13 definitions, 16 seeds each. Checks per definition: `progressive-market` 5, `sequential-cards` 23, `staged-survival` 12, `permutation` 12. Every report carries a `ran` map so a listed check cannot be an unrun one. All 13 reports `ok: true`                                                                                                                          |
| Hosted GitHub Actions                                                                         | `.github/workflows/ci.yml`                                                                                                                                                                                       | runs typecheck + vitest on push across Node 20/22/24; last recorded hosted run was infrastructure-blocked by an account spending limit                                                                                                                                                                                                                                                                  |
| Multi-position rounds are not crushed by a single-basis cap                                   | `tests/lifecycle-module-contract.test.ts`                                                                                                                                                                        | stakes of 1 then 10,000 under a 10x cap credit the full 38,800 claim, and stay inside `capBasisStake * maxWinMultiple`                                                                                                                                                                                                                                                                                  |
| Hostile-input behaviour is unchanged from `main` after the relocation                         | side-by-side run of `uniform` on eight malformed contexts in a worktree                                                                                                                                          | identical error code and path on every case (`INVALID_CONTEXT`, `INVALID_ADAPTER`, `UNSUPPORTED_VERSION`)                                                                                                                                                                                                                                                                                               |
| Seeded protocol stress, per-module digests gated against their baselines                      | `npm run test:stress`, `artifacts/stress-v3.json`                                                                                                                                                                | 500 rounds / 5,538 operations; `moduleDigests` for `progressive-market` and `staged-survival` both match the committed baseline; a corrupted baseline was verified to fail the run                                                                                                                                                                                                                      |
| Transcript/posterior/proof benchmark                                                          | `npm run bench`, `artifacts/benchmark-v3.json`                                                                                                                                                                   | 1,500 samples / 52,721 events; both committed `moduleDigests` unchanged                                                                                                                                                                                                                                                                                                                                 |
| `sequential-cards` and `permutation` carry no stress or benchmark replay anchor               | `artifacts/stress-v3.json`, `artifacts/benchmark-v3.json`                                                                                                                                                        | **not closed here.** Both artifacts' `moduleDigests` hold two keys, not four: neither module joined the seeded workload, so their replay behaviour is pinned only by their frozen fixtures and conformance runs. Adding them is a workload change and a deliberate re-baseline                                                                                                                          |
| Production RGS, certification, regulatory approval, and production capacity                   | none                                                                                                                                                                                                             | not performed and not claimed                                                                                                                                                                                                                                                                                                                                                                           |

## `sequential-cards`

| Claim / control                                                               | Reproducible evidence                                                                                                                       | Observed result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `cards-transcript-v1` and `cards-book-v1` are frozen too                      | `tests/sequential-cards/frozen-fixtures.test.ts`                                                                                            | 8 tests over one committed round covering a two-row ticket, a reveal, a switch, and a settlement; 9 re-sealed mutations rejected on their merits, including a rewritten reveal, a rewritten round identity, and an emptied decision log. `cards-book-dormant-v1.json` freezes the same round closed by the **system** instead, with its settlement reason and the fingerprint that seals it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| A second, independently coded oracle over a whole card space                  | `tests/sequential-cards/oracle-three-card.test.ts`                                                                                          | all 1,716 ordered deals x 2 sealed selectors x 3 backed positions, from the closed form rather than from the module: every posterior in 3,432 leaves, all 26 published prices, the expected value of every offered action in every state, the argmin and argmax over all `2^38` legal policies (both `24/25`), both variance extremes (`6048/6875` and `1057392/40625`), all 14 side-market multipliers, the reachable maximum `648/5` and minimum `12/275`, and position invariance                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Independent adversarial review, round one                                     | GPT-5.6 at read-only over `src/modules/sequential-cards/`                                                                                   | 5 findings: 3 fixed (aggregate flooring at settlement, a settled snapshot trusting its own outcome, an unbounded ticket pre-pass), 1 fixed by narrowing (`liquidationSpread` restricted to zero, because above zero the reported worst policy was not an argmin), 1 documented rather than fixed (an unkeyed snapshot cannot be authenticated by re-derivation); each is recorded in `docs/adr/0005-…` Decision 6 with the scenario that produced it                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Independent adversarial review, round two                                     | second read-only review over the finished module                                                                                            | 8 findings (4 major, 4 minor), all fixed: commands re-reading the caller's request across the ledger's `await` (a row inflating its stake on the eighth read priced a claim 40,000,000x the validated one), `restore()` replaying receipt algebra without the round's own rules, a raw `TypeError` escaping `restore()`, and an identical-action enumeration nothing read; recorded in `docs/adr/0005-…` Decision 8                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Independent adversarial review, round three                                   | third read-only review over the module and its documents                                                                                    | 5 findings (2 major, 3 minor), all fixed: the analysis ceilings did not bound the wall time they published (missing `cardsBelief` term, cell budget still discovered from inside the walk, calibration measured on the loosest shape), the reveal-provenance boundary was stated only in a docstring, and four figures in this ledger were stale by one commit. Every one was a claim outrunning the code, not a broken control; recorded in `docs/adr/0005-…` Decision 9                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Restore rules, the frame pairing, and the settlement draw                     | `tests/sequential-cards/restore-rules.test.ts`, `tests/sequential-cards/settlement-draw.test.ts`                                            | 7 + 11 focused cases. Every forged snapshot asserts the **named guard** that refuses it, so a case cannot survive the deletion of the guard it is about; the frame rule is exercised on a two-reveal definition where a middle revision exists to be wrong about, with the honest frame restoring as the positive control; and the one residual §6.3 admits under the draw is pinned as a checked claim — a coordinated tape rewrite restores, credits exactly one credit more, and is refused by `settle`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Independent adversarial review, round four (closure)                          | fourth read-only review over the module, its documents and the consuming spec                                                               | 6 findings (1 blocker, 3 major, 2 minor), all closed: `restore()` credited a liquidation no round could have issued — 4,320 credits on a 100-credit stake where the honest figure is 196 — because the `cash` branch replayed no rules and nothing pinned a receipt's frame; the declared `steps.price` hook priced unvalidated step lists; §6.3 claimed to defeat every illegal rewrite while that one existed; the tamper table had no pairing case and no `cash` case at all; `rounding: 'stochastic'` was refused rather than implemented; and the definition-time walk could not yield. Recorded in `docs/adr/0006-…`                                                                                                                                                                                                                                                                                                                                                                                    |
| The dormant settlement, and the clock the module still does not own           | `tests/sequential-cards/dormancy.test.ts`, `tests/sequential-cards/hostile-input.test.ts`, `tests/sequential-cards/mutable-request.test.ts` | 21 focused cases, plus the hostile-input and mutable-request suites extended to the new command — every non-integer window and every unknown reason refused under its **named** reason, and a request that clears the window and then rewrites itself settles on what was validated. The 21: the consuming game's declared window refused a second early and accepted at it; the early reason accepted only where declared; a round before its first reveal refused live and in a forged snapshot; the credited integer swept against the `cash` the player did not take on 24 seeds (equal on every one that offers a liquidation, plus the markets' own settlement); a two-reveal definition settled dormant part-way through its schedule; a row liquidated in the same window it split in, at exactly the fair value it split from (equal rationals over 40 rounds); and every rewrite of the sealed reason — relabelled, erased, invented, and carried on a definition that declares no policy — refused |
| The consuming game's §4.1 definition constructs **as written**                | `tests/sequential-cards/settlement-draw.test.ts`                                                                                            | the declaration is transcribed field for field from `triad/docs/ENGINE.md` with nothing removed — `rounding: 'stochastic'` and `dormancy` included — built, and asserted to fingerprint identically to `triad-dormant-v1`. Two earlier rounds could only make this claim after deleting a field from the spec                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Independent adversarial review, round five (closure)                          | fifth read-only review over the module, its documents and the consuming spec                                                                | 3 findings (1 major, 2 minor), all closed: the consuming game's worked definition still did not construct because `dormancy` was refused by name — on a ground the spec itself contradicts, since it never asked the module to own a clock; one published `npm test` figure in this ledger was stale by a commit; and `CARDS_CAP_NEVER_BINDS` compared the claim ceiling while the construction gate it shares a name with compared the credit ceiling, so the check was weaker than the gate. Recorded in `docs/adr/0007-…`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Definition-time analysis ceilings bound wall time, and refusals are immediate | `node --import tsx scripts/analysis-calibration.ts`                                                                                         | committed probe table across both axes: the slowest shape both ceilings admit walks in ~13 s (`size 70 / dealt 3 / split`, 492,660 cells; 12.5-14 s across runs), a 52-card / 3-card deck in ~5 s, and all 4 refused shapes are refused in under 1 ms. Worst rate ~1,480 ns per estimated operation, on the shape where the bound is tightest — the figure the ceilings are set from                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

## `staged-survival`

| Claim / control                                                         | Reproducible evidence                                                                                                                                        | Observed result                                                                                                                                                                                                                                                                                                                                     |
| ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Exhaustive oracle against an independently coded model                  | `tests/staged-survival-oracle.test.ts`                                                                                                                       | 8 tests; a three-entity, two-stage instance enumerated over its whole elementary-event space — survivor law term for term, `E[survivors]`, `p * mu == 1`, the round EV of every contract path under every banking policy, and the cap held across the whole chain                                                                                   |
| Module surface, adapter refusals, tape completeness, book lifecycle     | `tests/staged-survival-module.test.ts`                                                                                                                       | 39 tests                                                                                                                                                                                                                                                                                                                                            |
| Hostile inputs at the transcript, snapshot and pricing boundaries       | `tests/security/staged-survival-hostile-input.test.ts`                                                                                                       | 34 tests; every re-sealed money-bearing snapshot rewrite rejected on its merits, including a forged **pending** decision whose `choose` receipt fingerprint was repointed to match it                                                                                                                                                               |
| No exported entry point throws an untyped error                         | `tests/security/staged-survival-hostile-input.test.ts`                                                                                                       | a sweep of 24 exported entry points against a 9-value junk matrix asserts only that whatever escapes is a `RevealEngineError`; an independent sweep found 47 `TypeError`/`RangeError` escapes before this round and finds 0 after                                                                                                                   |
| Exported helpers refuse before they allocate                            | `tests/security/staged-survival-hostile-input.test.ts`                                                                                                       | `laneSizes(narrow, 3e7)` allocated 30,000,000 elements and `1e9` died with a bare `RangeError` after ~5s; both are now `INVALID_CHOICE` in under 100 ms, asserted with a clock                                                                                                                                                                      |
| The documented fixture regeneration path leaves the tree clean          | `npm run fixtures:update && git status --porcelain && npm run format:check`                                                                                  | no modified files, and prettier reports all matched files formatted                                                                                                                                                                                                                                                                                 |
| The money path is total over every stake `enter()` accepts              | `tests/staged-survival-module.test.ts`, `tests/security/staged-survival-hostile-input.test.ts`                                                               | `SURVIVAL_LIMITS.maxStakeBits` (64) is reserved inside the define-time pricing bound; `2^64 - 1` opens a round that restores, `2^64` and `2^4090` are refused with `CLAIM_REJECTED` before any mutation (no claim, no receipt, no cap basis), and a restored entry stake is held to the same width                                                  |
| The define-time width bound is not so blunt that it refuses real work   | `tests/staged-survival-module.test.ts`                                                                                                                       | a 32-entity field in one width-32 lane at 60-bit denominators defines, and `survivorDistribution` returns 33 terms summing to exactly `1` with the closed-form mean                                                                                                                                                                                 |
| Twelve conformance checks over both references                          | `npm run conformance`                                                                                                                                        | per definition: 12 checks, 1 definition-scoped run + 16 per round-scoped check. Across the two references: 18 fairness identities, 18 distributions, 18 lane geometries, 64 tapes, 96 choice logs, 64 commitment bodies, 96 seed commitments, 32 transcripts, 22 tampered transcripts, 32 snapshots, 156 tampered snapshots, 32 conservation checks |
| Bounded load, correctness digest gated against its baseline             | `npm run test:stress`, `artifacts/stress-v3.json`                                                                                                            | 100 survival rounds inside the 500-round workload: 432 entries, 222 decisions and stages, 36 banks, 25 reconnects, 100 settlements + 100 idempotent replays, 100 replayed steps refused, 15 tampered commitments rejected; `moduleDigests['staged-survival']` matches the committed baseline                                                        |
| Throughput of the build-and-verify path                                 | `npm run bench`, `artifacts/benchmark-v3.json`                                                                                                               | 300 survival samples / 712 stages inside a 1,500-sample run; whole run 52,712 events at ~24,200 events/s, p50/p95/p99 0.344/3.820/4.372 ms                                                                                                                                                                                                          |
| Adding a second module did not disturb the first module's replay anchor | `npm run test:stress`, `npm run bench`                                                                                                                       | `moduleDigests['progressive-market']` is `fdb9e9b8…baef1` (stress) and `f8b8d166…7fa8` (benchmark), byte-identical to the values the single-module `-v2` artifacts carried                                                                                                                                                                          |
| The digest gate fails on drift and on a silently dropped module         | corrupt one anchor, then remove one, and re-run; `tests/artifact-schema.test.ts`                                                                             | both fail the run with exit code 1 and name the module; `compareModuleDigests()` is unit-tested over the union of both key sets, and the gate validates its own baseline with `assertStressArtifact` and fails rather than skipping when that baseline is malformed                                                                                 |
| No core file was modified by this module                                | `git diff platform/core...platform/staged-survival -- src/core src/api src/protocol src/serialization src/conformance src/integration src/reference src/cli` | empty; the sole pre-existing `src/` change is the module registry entry. See `docs/adr/0008-round-entropy-without-a-core-change.md`                                                                                                                                                                                                                 |

## `permutation`

| Claim / control                                                                 | Reproducible evidence                                                                                                                                                            | Observed result                                                                                                                                                                                                                                                                                                                                                       |
| ------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Module surface, definition refusals, book binding and restore rules             | `tests/permutation-module.test.ts`                                                                                                                                               | 169 tests. Includes the two declared narrowings: `book.create(definition)` returns an unbound book that refuses every bet and `book.restore(definition, snapshot)` restores only an unbound snapshot, both asserted rather than assumed, so a host driving the registry polymorphically finds the refusal instead of a weaker reconstruction                          |
| Independently coded oracle over the paytable and the counting                   | `tests/permutation-oracle.test.ts`                                                                                                                                               | 25 tests priced from the closed form rather than from the module                                                                                                                                                                                                                                                                                                      |
| The sampler is unbiased and the shuffle is uniform                              | `tests/sampler-unbiased.test.ts`, `tests/shuffle-uniformity.test.ts`                                                                                                             | 18 + 12 tests                                                                                                                                                                                                                                                                                                                                                         |
| `permutation-transcript-v1` and `permutation-book-v2` are frozen                | `tests/permutation-frozen-fixtures.test.ts`                                                                                                                                      | 10 tests. `permutation-book-v1.json` is kept as a **negative** fixture for the retired unbound schema and asserted to be refused with `UNSUPPORTED_VERSION`; `npm run fixtures:update` deliberately does not regenerate it                                                                                                                                            |
| The AETHER ORDER engine contract is implemented byte for byte                   | `tests/aether-conformance.test.ts`, `tests/aether-receipt.test.ts`, `tests/aether-sampler.test.ts`, `tests/aether-hostile-input.test.ts`, `tests/aether-frozen-fixtures.test.ts` | 7 + 3 + 4 + 5 + 3 tests. `tests/fixtures/aether-order-transcripts.json` is a verbatim copy of the game repository's artefact, pinned by SHA-256 and excluded from prettier so reformatting cannot silently make it stop being a copy                                                                                                                                  |
| Twelve conformance checks over all three references                             | `npm run conformance`                                                                                                                                                            | `aether-order-classic`: 120 draw vectors, 175 instances, 400 price sweeps, 912 settled instances, 64 tampered transcripts, 256 tampered snapshots. `aether-order-seven`: 5,040 draw vectors, 5,145 instances, 784 price sweeps, 1,712 settled instances. `triad`: 6 draw vectors, 27 instances, 144 price sweeps, 368 settled instances. All three reports `ok: true` |
| Six of the eleven AETHER ORDER families are priced outside the lifecycle module | `src/modules/permutation/aether/`, `docs/modules/permutation.md` §11                                                                                                             | **declared, not a defect.** `before`, `early`, `late`, `neighbours`, `opening` and `podium` are priced by exact enumeration over the full `n!` space from adapter-supplied `resolve` predicates; the lifecycle module itself prices five, because a predicate it did not write cannot be reduced to the pairwise-exclusive pins it counts with                        |
| RGS-side pacing (`CYCLE_FLOOR`, `BETTING_CLOSED`)                               | none                                                                                                                                                                             | **not closed here.** The error codes exist so a host raises the right one; the enforcement needs session state neither repository has                                                                                                                                                                                                                                 |

## Reading these figures

Stress and benchmark timings are synthetic local measurements taken on one
machine state and Node build. Throughput here varies by roughly ±20% run to run,
so a single figure is a sample and not a claim. The replay anchors are the
per-module correctness digests and the workload seeds, not the timings.

`moduleDigests` replaced the single `correctnessDigest` of the `-v2` artifacts,
which is why both schemas are now `-v3`: one digest over a whole run moves
whenever a module is _added_ to the workload, which makes a new workload
indistinguishable from drift in an existing one. The 0.2/0.3 values are carried
forward unchanged under the `progressive-market` key, so the continuity that
digest established is intact and still checkable.

The `staged-survival` anchor was **deliberately re-baselined** in this round. The
sampler domain became the definition fingerprint rather than the definition id
(`docs/modules/staged-survival.md` §3), which moves every derived tape and
therefore every replayed round. The gate did its job — it refused the run and
named the module — and the baseline was regenerated only after that version
decision was recorded in the changelog. The evidence that the change is scoped is
in the same artifacts: `moduleDigests['progressive-market']` is byte-identical
across the re-baseline in both the stress and the benchmark run, so exactly one
module's replay behaviour moved.

Two of the four modules — `sequential-cards` and `permutation` — are not in the
stress or benchmark workload, so neither artifact carries a digest for them. That
is a gap in the replay evidence, not in the correctness evidence: both are pinned
by frozen fixtures compared byte for byte and by conformance runs that sweep
their tampers. Adding them to the workload is a deliberate re-baseline and is
left to the round that does it.

Hosted Actions state must be read from GitHub; infrastructure blocking is neither
a code pass nor a code failure.
