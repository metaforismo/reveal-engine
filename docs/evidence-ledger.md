# Evidence ledger

Repository-level engineering evidence from 2026-07-29, branch `platform/core`.
Every figure below was produced by the command in its row on this working tree.
It does not cross the certification boundary.

| Claim / control                                                                               | Reproducible evidence                                                       | Observed result                                                                                                                                                                                                  |
| --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Stable API, strict codecs, fixtures, exact math, protocol, replay, races, and hostile inputs  | `npm test`                                                                  | 18 files / 270 tests passed                                                                                                                                                                                      |
| Relocation and the cap-chain rework preserved behavior for unchanged adapters                 | `npm run test:stress`                                                       | stress `correctnessDigest` `fdb9e9b8…baef1` is byte-identical to the 0.2 baseline, and the run now fails if it ever is not                                                                                       |
| The only intended replay-visible change is the derived continuation policy                    | recompute the benchmark digest with `blackSignalReference` pinned to 1.0.0  | reproduces the 0.2 digest `dab610f4…0412` exactly; 0.3 digest `f8b8d166…7fa8` differs only through the 1.0.0 → 1.1.0 adapter bump                                                                                |
| Lifecycle-module contract is implementable by more than its first client                      | `tests/lifecycle-module-contract.test.ts`                                   | 22 tests; a test-only permutation-truth / zero-elimination / combinatorial-paytable / multi-claim module passes its own conformance run                                                                          |
| The contract carries a round whose transcript depends on player decisions                     | `tests/choice-timed-contract.test.ts`                                       | 11 tests; a `choiceTiming: 'before-step'` module with a seed pre-commitment, a bound choice log, and per-entity partial banking                                                                                  |
| Round payout never exceeds `maxWinMultiple` x money brought in                                | `tests/cap-chain-invariant.test.ts`                                         | invariant checked after every command over all 7,380 sequences up to length four, plus 5,000 seeded interleavings up to length 20; letting a recycled stake grow the basis was verified to fail it               |
| Multi-position rounds are not crushed by a single-basis cap                                   | `tests/lifecycle-module-contract.test.ts`                                   | stakes of 1 then 10,000 under a 10x cap credit the full 38,800 claim, and stay inside `capBasisStake * maxWinMultiple`                                                                                           |
| `receipt-v1` and `round-book-v1` are frozen, not round-tripped                                | `tests/frozen-fixtures.test.ts`, `tests/fixtures/`                          | 6 tests rebuild the round and compare committed bytes field for field; renaming one snapshot key was verified to break the run                                                                                   |
| Independent probability, pricing, strategy, rounding, cap, and accounting oracles             | `tests/math-oracle-invariants.test.ts`, `tests/protocol-accounting.test.ts` | exhaustive binary paths plus three-adapter raw-weight checks passed                                                                                                                                              |
| Deterministic property/metamorphic coverage                                                   | `tests/property-metamorphic.test.ts`                                        | 32 fixed property seeds and 256 replay/isolation cases passed                                                                                                                                                    |
| Core primitives: weights, counting, permutations, tapes, sealing, ledger                      | `tests/core-primitives.test.ts`                                             | 27 focused tests passed, including zero-elimination, cap-basis accumulation, and exact 1/336 trifecta pricing                                                                                                    |
| Proof vectors, migrations, tamper taxonomy, snapshot mutation, and hostile request validation | `npm run test:security`                                                     | 103 focused tests passed (65 hostile-input, 30 snapshot-mutation, 8 proof-vector), including 50 malformed-context and wire-helper cases added this round (hostile-input 15 -> 65); audit found 0 vulnerabilities |
| Hostile-input behaviour is unchanged from `main` after the relocation                         | side-by-side run of `uniform` on eight malformed contexts in a worktree     | identical error code and path on every case (`INVALID_CONTEXT`, `INVALID_ADAPTER`, `UNSUPPORTED_VERSION`)                                                                                                        |
| Coverage gate                                                                                 | `npm run test:coverage`                                                     | statements 89.32%, branches 85.59%, functions 96.82%, lines 90.78%                                                                                                                                               |
| Module generality and conformance                                                             | `npm run conformance`                                                       | registry-driven: every module x every declared reference. 3 materially different definitions, 16 seeds each, 4 checks each, 144 truths swept                                                                     |
| Build and package surface                                                                     | `npm run typecheck && npm run build && npm run test:package`                | tarball installed into a throwaway consumer; 10 subpaths resolved through the published `exports` map; 95 packed files; no src/tests                                                                             |
| Seeded protocol stress, digest gated against its baseline                                     | `scripts/stress.ts`, `artifacts/stress-v2.json`                             | 500 rounds / 4,334 operations; 18,742 ops/s; p50/p95/p99 0.011/0.112/0.192 ms; `correctnessDigest` matches the committed baseline; a corrupted baseline was verified to fail the run                             |
| Transcript/posterior/proof benchmark                                                          | `scripts/benchmark.ts`, `artifacts/benchmark-v2.json`                       | 1,200 samples / 52,000 events; 31,011 events/s; p50/p95/p99 0.293/4.170/4.749 ms; digest `f8b8d166…7fa8` unchanged                                                                                               |
| Hosted GitHub Actions                                                                         | `.github/workflows/ci.yml`                                                  | runs typecheck + vitest on push across Node 20/22/24; last recorded hosted run was infrastructure-blocked by an account spending limit                                                                           |
| Production RGS, certification, regulatory approval, and production capacity                   | none                                                                        | not performed and not claimed                                                                                                                                                                                    |

Stress and benchmark figures are synthetic local measurements and are **not**
comparable to the 0.2 baselines: they were taken on a different machine state
and Node build. The replay anchors are the correctness digests and the workload
seeds, not the timings. Hosted Actions state must be read from GitHub;
infrastructure blocking is neither a code pass nor a code failure.
