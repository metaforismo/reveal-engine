# Evidence ledger

Repository-level engineering evidence from 2026-07-29, branch `platform/core`.
It does not cross the certification boundary.

| Claim / control                                                                               | Reproducible evidence                                                       | Observed result                                                                                                                            |
| --------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| Stable API, strict codecs, fixtures, exact math, protocol, replay, races, and hostile inputs  | `npm test`                                                                  | 15 files / 185 tests passed                                                                                                                |
| Relocation preserved behavior for unchanged adapters                                          | `npm run test:stress`                                                       | stress `correctnessDigest` `fdb9e9b8…baef1` is byte-identical to the 0.2 baseline                                                          |
| The only intended replay-visible change is the derived continuation policy                    | recompute the benchmark digest with `blackSignalReference` pinned to 1.0.0  | reproduces the 0.2 digest `dab610f4…0412` exactly; 0.3 digest `f8b8d166…7fa8` differs only through the 1.0.0 → 1.1.0 adapter bump          |
| Lifecycle-module contract is implementable by more than its first client                      | `tests/lifecycle-module-contract.test.ts`                                   | 19 tests; a test-only permutation-truth / zero-elimination / multi-claim module passes `defineLifecycleModule` and its own conformance run |
| Independent probability, pricing, strategy, rounding, cap, and accounting oracles             | `tests/math-oracle-invariants.test.ts`, `tests/protocol-accounting.test.ts` | exhaustive binary paths plus three-adapter raw-weight checks passed                                                                        |
| Deterministic property/metamorphic coverage                                                   | `tests/property-metamorphic.test.ts`                                        | 32 fixed property seeds and 256 replay/isolation cases passed                                                                              |
| Core primitives: weights, permutations, tapes, sealing, ledger                                | `tests/core-primitives.test.ts`                                             | 19 focused tests passed, including zero-elimination and chain-cap exhaustion                                                               |
| Proof vectors, migrations, tamper taxonomy, snapshot mutation, and hostile request validation | `npm run test:security`                                                     | 53 focused tests passed, including 30 re-sealed snapshot mutations; runtime dependency audit found 0 vulnerabilities                       |
| Coverage gate                                                                                 | `npm run test:coverage`                                                     | statements 87.36%, branches 82.85%, functions 94.19%, lines 89.20%                                                                         |
| Module generality and conformance                                                             | `npm run conformance`                                                       | 3 materially different definitions, 16 seeds each, 16 verified transcripts and 4 declared checks each                                      |
| Build and package surface                                                                     | `npm run typecheck && npm run build && npm run test:package`                | tarball installed into a throwaway consumer; 10 subpaths resolved through the published `exports` map; 93 packed files; no src/tests       |
| Seeded protocol stress                                                                        | `scripts/stress.ts`, `artifacts/stress-v2.json`                             | 500 rounds / 4,334 operations; 23,966 ops/s; p50/p95/p99 0.008/0.083/0.145 ms; 2,047,736-byte heap delta; schema and thresholds passed     |
| Transcript/posterior/proof benchmark                                                          | `scripts/benchmark.ts`, `artifacts/benchmark-v2.json`                       | 1,200 samples / 52,000 events; 37,037 events/s; p50/p95/p99 0.234/3.556/4.429 ms; schema and thresholds passed                             |
| Hosted GitHub Actions                                                                         | `.github/workflows/ci.yml`                                                  | runs typecheck + vitest on push across Node 20/22/24; last recorded hosted run was infrastructure-blocked by an account spending limit     |
| Production RGS, certification, regulatory approval, and production capacity                   | none                                                                        | not performed and not claimed                                                                                                              |

Stress and benchmark figures are synthetic local measurements and are **not**
comparable to the 0.2 baselines: they were taken on a different machine state
and Node build. The replay anchors are the correctness digests and the workload
seeds, not the timings. Hosted Actions state must be read from GitHub;
infrastructure blocking is neither a code pass nor a code failure.
