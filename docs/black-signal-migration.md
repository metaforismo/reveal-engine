# BLACK SIGNAL adoption plan

1. Freeze and independently verify current BLACK SIGNAL behavior against its own repository.
2. Map its four outcomes, equal priors, 120-event weak/spike schedule, first-entry RTP, and cap policy to `blackSignalReference`.
3. Replace only the server-side math/fairness boundary behind contract tests; do not import this repository's references into its UI bundle.
4. Run dual transcript/price replay in shadow mode, then perform operator and game-math review.
5. Make continuation, rounding and cap differences explicit in release notes. No migration implies certification or unchanged RTP after real payable rules.
